# 知识库召回与上下文构建设计文档

## 1. 文档目标

本文档定义测试 Agent 中“知识库召回与上下文构建”的读路径设计，用于说明：

- 知识库如何检索
- 不同阶段如何切换召回策略
- 如何将检索结果组装成 Agent 真正可用的上下文

目标不是“把更多文档塞给模型”，而是：

`把当前任务最相关、最可信、最值得关注的一小部分知识，精准组装进上下文`

---

## 2. 在整体架构中的位置

知识库召回应复用统一的上下文组装思路，而不是各模块各自搜索。

整体关系如下：

```text
Current Task / ApiSpec / ExecutionResult / FailureInfo
-> KnowledgeQueryBuilder
-> KnowledgeRetriever
-> KnowledgeReranker
-> KnowledgeContextBuilder
-> ContextBundle
```

与其他模块关系：

- `记忆系统`：负责经验召回
- `知识库召回`：负责项目知识召回
- `Unified Context Builder`：负责最终合并

建议方式是：

`一个统一上下文框架 + 多个阶段化召回 profile`

---

## 3. 核心设计原则

### 3.1 召回必须阶段化

接口分析、case 生成、失败分析，关注的信息完全不同。

因此不能所有阶段都用同一组召回策略。

### 3.2 召回必须混合检索

测试场景里很多知识都带结构约束，比如：

- 具体系统
- 具体模块
- 具体接口
- 具体错误码

所以召回必须混合使用：

- 结构过滤
- 标签过滤
- 向量召回

### 3.3 上下文构建必须压缩

召回结果不应直接把原文整块塞给模型。

应做：

- 去重
- 裁剪
- 摘要
- 分类

### 3.4 可信度要参与排序

文档不是只看相似度，还要看：

- 是否官方
- 是否最新
- 是否适用于当前阶段

---

## 4. 总体设计

建议采用：

`查询构建 + 多路召回 + 重排 + 分类摘要 + 上下文组装`

的五段式流程。

主流程如下：

```text
Task Context
-> KnowledgeQueryBuilder
-> Structure Filter
-> Tag Filter
-> Semantic Recall
-> KnowledgeReranker
-> KnowledgeContextBuilder
-> ContextBundle
```

流程说明：

1. 根据当前阶段构建检索 query
2. 做元数据结构过滤
3. 做标签缩小范围
4. 做语义召回
5. 对候选结果做重排
6. 组装为阶段化上下文块

---

## 5. 阶段化召回策略

第一版建议至少支持 3 个召回 profile：

- `API_ANALYSIS_PROFILE`
- `CASE_GENERATION_PROFILE`
- `FAILURE_ANALYSIS_PROFILE`

### 5.1 API_ANALYSIS_PROFILE

目标：

- 帮助接口分析理解业务背景和流程位置

优先召回：

- `business_flow`
- `api_note`
- `env_guide`
- `domain_rule`

重点字段：

- `system`
- `module`
- `apiPath`
- `httpMethod`
- `bizEntity`

### 5.2 CASE_GENERATION_PROFILE

目标：

- 帮助测试用例生成补足业务深度和测试规范

优先召回：

- `test_spec`
- `incident_postmortem`
- `domain_rule`
- `api_note`

重点字段：

- `module`
- `bizEntity`
- `riskTags`
- `caseType`

### 5.3 FAILURE_ANALYSIS_PROFILE

目标：

- 帮助失败分析识别相似故障与已有解释

优先召回：

- `error_code_guide`
- `incident_postmortem`
- `env_guide`
- `api_note`

重点字段：

- `errorCode`
- `errorMessage`
- `apiPath`
- `system`

---

## 6. Query 构建设计

建议通过 `KnowledgeQueryBuilder` 根据当前阶段生成查询对象。

### 6.1 API 分析阶段 Query

建议输入：

- `system`
- `module`
- `apiPath`
- `httpMethod`
- `bizEntity`
- `controllerName`

### 6.2 Case 生成阶段 Query

建议输入：

- `system`
- `module`
- `bizEntity`
- `caseType`
- `riskTags`
- `apiSummary`

### 6.3 失败分析阶段 Query

建议输入：

- `system`
- `module`
- `apiPath`
- `errorCode`
- `errorMessage`
- `assertionFailureSummary`

---

## 7. 多路召回策略

建议第一版采用 4 路组合：

- `结构过滤召回`
- `标签过滤召回`
- `语义召回`
- `时间/权威性增强`

### 7.1 结构过滤召回

先按元数据过滤候选集。

建议过滤字段：

- `system`
- `module`
- `docType`
- `bizEntity`
- `applicableStages`

这一步的目标是缩小范围，而不是排序。

### 7.2 标签过滤召回

再按标签增强匹配。

可用标签包括：

- `auth`
- `payment`
- `order`
- `risk`
- `validation`
- `regression`

### 7.3 语义召回

在过滤后的候选集中做 embedding 检索。

第一版建议：

- query 做 embedding
- chunk 做 embedding
- topK 召回

### 7.4 时间与权威性增强

最后对文档增加两个附加因子：

- 是否最近更新
- 是否高权威来源

---

## 8. 重排策略设计

建议第一版使用线性加权方式做重排，先保证简单可控。

可参考公式：

```text
finalScore =
0.30 * structureMatch
+ 0.25 * tagMatch
+ 0.30 * semanticScore
+ 0.15 * authorityFreshnessScore
```

不同阶段可以调权重：

- `API_ANALYSIS_PROFILE`：结构权重更高
- `CASE_GENERATION_PROFILE`：标签和风险权重更高
- `FAILURE_ANALYSIS_PROFILE`：错误码和语义相似度权重更高

---

## 9. 上下文构建设计

召回完成后，不建议直接输出原文列表。

建议 `KnowledgeContextBuilder` 输出以下结构化块：

- `businessFlowSummary`
- `ruleSummary`
- `riskSummary`
- `testHintSummary`
- `referenceSnippets`

### 9.1 businessFlowSummary

总结当前接口所在的业务流程、上下游依赖和状态位置。

### 9.2 ruleSummary

总结关键业务规则、鉴权规则、输入约束。

### 9.3 riskSummary

总结历史事故、高风险模式和特别关注点。

### 9.4 testHintSummary

总结建议的测试关注方向。

例如：

- 推荐补边界值
- 推荐补鉴权失败场景
- 推荐补状态流转异常场景

### 9.5 referenceSnippets

保留少量原始证据片段，便于：

- 可解释性
- 报告展示
- 人工复核

---

## 10. 与 Unified Context Builder 的关系

知识库召回结果不应该单独直接给 Agent。

建议最终进入统一上下文组装流程：

```text
KnowledgeContext
+ MemoryContext
+ CodeContext
+ CurrentTaskContext
-> Unified Context Builder
-> ContextBundle
```

这样每个阶段都可以拿到统一结构，而不是自己拼 prompt。

---

## 11. 第一版结果数量控制

为了控制上下文体积，建议第一版限制召回与注入量。

建议配置：

- 每阶段最终注入 chunk 不超过 `5~8`
- 每类摘要不超过 `3` 条核心点
- 原始引用片段总长度受控

目标是：

`少而准，而不是多而杂`

---

## 12. 失败与降级策略

知识库召回失败时，不应该阻塞主流程。

建议降级顺序：

1. 先结构过滤 + 标签过滤
2. 如果 embedding 不可用，只返回结构匹配结果
3. 如果知识库为空，则只依赖代码上下文与记忆系统

也就是说：

`知识库增强是强增益，但不应成为单点阻塞`

---

## 13. 第一版落地建议

建议第一版按以下顺序实现：

1. 定义 `KnowledgeQuery`、`KnowledgeHit`、`KnowledgeContext`
2. 实现 3 个阶段化 profile
3. 实现结构过滤
4. 实现标签过滤
5. 接入向量召回
6. 实现线性重排
7. 实现 `KnowledgeContextBuilder`
8. 接入 `Unified Context Builder`

第一版优先验证 3 个场景：

- 接口分析时自动补业务流程
- 用例生成时自动补测试规范
- 失败分析时自动补错误码说明与事故复盘

---

## 14. 一句话总结

知识库召回与上下文构建的核心价值是：

`按任务阶段从项目知识中精准取回最相关、最可信的一小部分内容，并压缩成 Agent 可直接消费的结构化上下文`
