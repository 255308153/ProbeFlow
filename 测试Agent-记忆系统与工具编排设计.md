# 测试 Agent 记忆系统与工具编排设计

## 目标

这套设计面向测试平台 Agent，核心目标是支撑以下三类能力：

- 生成功能测试用例
- 生成接口测试用例
- 执行自动化接口测试，并基于结果继续分析和修正

整体思路可以概括为：

`Mem0 风格记忆提纯 + Viking 风格统一上下文 + Agent Workflow 主编排 + DAG 子流程`

---

## 一、记忆系统设计

### 1. Session Memory

Session Memory 用来保存当前会话的短期上下文，主要服务于“这一轮正在做什么”。

建议存放内容：

- 当前需求描述、当前任务目标
- 最近几轮对话中的关键结论
- 最近一次工具调用结果
- 最近一次失败原因与重试状态
- 当前阶段的决策痕迹

它的特点是时效性强、生命周期短，不追求长期保留。

### 2. Task Memory

Task Memory 是当前测试任务的主记忆，围绕“一个任务对象”持续积累信息。

建议存放内容：

- 任务基础信息：需求、接口、模块、优先级
- 已生成的测试用例
- 已执行的接口测试记录
- 断言结果、失败样本、异常响应
- 分析结论和下一步动作

对于测试 Agent 来说，Task Memory 比普通聊天记忆更重要，因为很多信息必须在一次完整任务闭环里持续追踪，而不能只依赖 prompt 临时拼接。

### 3. Memory Refinery

这一层借鉴 Mem0 的“记忆提纯”思想。

核心原则不是“全量存储”，而是“提炼后再写入”。它的职责包括：

- 从会话和任务结果中抽取真正有复用价值的信息
- 对记忆做分类，例如经验、偏好、项目知识、常见失败模式
- 去重、合并、压缩，避免长期记忆不断膨胀
- 过滤一次性、噪音化、低价值内容

适合沉淀的内容包括：

- 某类接口常见断言模式
- 某个项目的鉴权/签名特殊规则
- 某类失败经常由哪些字段缺失导致
- 用户或团队的测试偏好与规范

### 4. Refined Long-term Memory

这是经过提纯后的长期记忆层，保存跨任务仍然有价值的经验。

建议分成几类：

- `project_knowledge`：项目知识，如接口约定、模块特性、领域规则
- `testing_pattern`：可复用的测试策略、断言模板、覆盖套路
- `failure_pattern`：常见失败模式、排查经验、修复建议
- `preference`：用户偏好、团队风格、报告输出偏好

这一层不应该保存原始大段对话，而应该保存“可复用、可检索、可装配”的结构化知识。

### 5. Unified Context Builder

这一层借鉴 Viking 的“统一上下文”思想，负责读路径，而不是写路径。

它的职责不是简单检索，而是将多种来源的上下文统一装配成当前任务输入：

- Session Memory
- Task Memory
- Refined Long-term Memory
- Team Knowledge Memory（可选）
- 外部任务材料，如需求文档、OpenAPI、历史执行报告

统一上下文的目标是“按需召回、统一组装”，而不是把所有记忆全量塞进 prompt。

可以把它理解成当前 Agent 的上下文构建器，它决定：

- 这次生成测试用例时，需要带哪些历史规则
- 这次执行接口测试时，需要带哪些环境信息
- 这次分析失败结果时，需要召回哪些经验模式

### 6. 可选：Team Knowledge Memory

这一层适合做人可维护的共享知识库，建议保留为 Markdown / Docs 形态。

适合放的内容：

- 团队测试规范
- 常见环境问题说明
- 某项目的接口使用约束
- 历史事故复盘沉淀
- 特殊测试策略说明

它和长期记忆的区别在于：

- Long-term Memory 更偏 Agent 自动沉淀
- Team Knowledge 更偏人工维护与共享协作

### 7. 记忆系统设计原则

- 当前轮次强相关的信息，优先放 Session Memory
- 当前任务持续演进的信息，优先放 Task Memory
- 跨任务复用的经验，经过提纯后写入 Long-term Memory
- 团队规范、项目文档、可人工维护知识，放 Team Knowledge Memory
- 读取时由 Unified Context Builder 统一组装，不做无差别全量注入

---

## 二、工具编排系统设计

### 1. 总体思路

对于测试 Agent，不建议只用纯 DAG 做主编排。

更合适的方式是：

- 主编排采用“有状态的 Agent Workflow”
- 稳定、重复、可批量化的子流程采用“DAG / Job Flow”

原因在于测试 Agent 不是单向流水线，它经常需要形成闭环：

`分析 -> 生成 -> 执行 -> 观察 -> 再决策 -> 再执行`

这类流程天然更接近 Agent 工作流，而不是静态 DAG。

### 2. 为什么不是纯 DAG

纯 DAG 适合确定性流程，但测试 Agent 存在大量动态分支：

- 用例生成后，可能发现输入信息不足，需要补充分析
- 接口执行失败后，可能要决定是重试、补断言还是回退
- 测试结果异常时，可能要重新规划下一批执行任务

因此主编排层必须具备：

- 状态感知
- 基于观察结果继续决策
- 工具选择能力
- 多轮循环能力

### 3. 为什么仍然保留 DAG 思想

虽然主编排不建议做成纯 DAG，但 DAG 对测试平台仍然非常有价值，特别适合以下子场景：

- 批量接口测试执行
- 并行运行多个测试用例
- 数据准备 -> 执行 -> 汇总报告 这类固定流程
- 多环境回归任务
- 夜间批处理任务

所以更合理的架构是：

- 上层 Agent 负责决策闭环
- 下层 DAG 负责稳定执行

### 4. 编排模块拆分建议

#### Planner

负责把用户目标转成任务计划，例如：

- 先分析需求或 OpenAPI
- 再生成接口测试用例
- 再挑选执行策略
- 最后汇总结果并生成报告

#### Tool Router

负责在不同阶段选择合适工具，例如：

- 文档解析工具
- OpenAPI 解析工具
- 用例生成工具
- HTTP 执行工具
- 结果分析工具
- 报告生成工具

#### Execution Controller

负责真正发起执行，并管理超时、并发、重试、取消等控制逻辑。

#### Observation Collector

负责收集执行结果，并形成标准化观察数据，例如：

- 响应码
- 响应体摘要
- 断言通过/失败
- 错误日志
- 环境信息

#### Retry / Recovery

负责失败后的恢复策略，例如：

- 是否重试
- 是否切换备用参数
- 是否降低执行粒度
- 是否把问题上抛给 Planner 重新规划

#### Task State Store

负责保存任务全局状态，使整个 Agent 不是“每轮都重新开始”，而是能持续推进。

### 5. 典型执行流程

一个典型的测试 Agent 工作流可以是：

1. 输入需求文档、接口文档或 OpenAPI
2. Planner 分析任务目标和可执行范围
3. 生成功能测试用例或接口测试用例
4. Execution Controller 调用接口执行工具发起测试
5. Observation Collector 收集结果
6. Planner 根据结果判断是否需要补充断言、补数据、重试或继续扩展用例
7. 最终输出测试报告、问题清单和可复用结论

这个流程的关键点不在于“一次跑完”，而在于“根据执行结果继续调整”。

### 6. DAG 化子流程示例

以下场景很适合收敛成 DAG 或 Job Flow：

- 批量生成某组接口的基础测试用例
- 并行执行多个接口测试 case
- 先准备测试数据，再执行，再做报告聚合
- 按环境并发回归
- 夜间定时跑自动化接口回归

也就是说，DAG 不应该消失，而应该下沉为执行层能力。

---

## 三、记忆系统与编排系统的职责边界

### 1. 记忆系统负责什么

记忆系统负责回答两个问题：

- 哪些信息值得留下
- 当前任务该拿哪些历史信息回来用

它的重点是“沉淀”和“召回”。

### 2. 编排系统负责什么

编排系统负责回答另外两个问题：

- 下一步做什么
- 下一步调用哪个工具、以什么顺序执行

它的重点是“决策”和“执行控制”。

### 3. 两者如何配合

推荐的数据流关系如下：

1. 编排系统执行任务
2. 执行结果进入 Task Memory
3. Memory Refinery 从中提取高价值经验
4. 提纯后的内容写入 Long-term Memory
5. 下一轮任务由 Unified Context Builder 按需召回
6. 编排系统基于新上下文继续决策

这能形成一个持续变强的闭环，而不是每次任务都从零开始。

---

## 四、第一版落地建议（Java 方向）

考虑到后续准备使用 Java 实现，第一版可以尽量保持“简单但可扩展”。

### 1. 记忆层建议

- `Session Memory`：Caffeine 或 Redis
- `Task Memory`：PostgreSQL
- `Refined Long-term Memory`：PostgreSQL + pgvector
- `Team Knowledge Memory`：Markdown 文档目录 + 项目知识库

说明：

- Session 追求快和短期可用
- Task 追求结构化和可追踪
- Long-term 追求检索和复用
- Team Knowledge 追求人可编辑、可审阅

### 2. 编排层建议

第一版不必强依赖某个复杂框架，可以先用 Java 自己实现清晰的服务分层：

- `PlannerService`
- `ContextBuilderService`
- `ToolRouterService`
- `ExecutionService`
- `ObservationService`
- `MemoryRefineryService`

在这个基础上，将任务状态抽象成一个有状态工作流。

如果后续规模变大，再逐步引入：

- 更明确的状态机模型
- DAG / Job 调度能力
- 并行执行引擎
- 定时回归调度能力

### 3. 数据对象建议

建议尽早定义几类核心对象：

- `Task`
- `TestCase`
- `ExecutionRecord`
- `Observation`
- `MemoryItem`
- `ContextBundle`
- `PlanStep`

这样后面无论接 LLM、工具系统还是调度层，都会更稳定。

---

## 五、后续扩展建议

### 1. codegraph 的位置

`codegraph` 更适合作为代码理解和上下文构建能力，而不是简单“装进 RAG 的原始数据”。

对测试 Agent 来说，它更适合扮演：

- 代码结构检索器
- 调用链分析器
- 上下文补全器

尤其在做接口测试生成、依赖分析、代码辅助定位时会有帮助。

### 2. 可继续扩展的工具能力

后续还可以增加：

- OpenAPI / Swagger 解析工具
- HTTP 接口执行工具
- 测试数据构造工具
- 报告生成工具
- 浏览器自动化工具
- 日志检索与失败归因工具

这会让测试 Agent 从“会写用例”逐步走向“能执行、能分析、能回收经验”。

---

## 一句话总结

这套方案的核心不是把所有能力都堆进一个大模型 prompt，而是把测试 Agent 拆成两条主线：

- 记忆系统负责“沉淀经验与统一上下文”
- 编排系统负责“任务决策与工具执行闭环”

最终形成：

`Mem0 风格记忆提纯 + Viking 风格统一上下文 + Agent Workflow 主编排 + DAG 子流程执行`
