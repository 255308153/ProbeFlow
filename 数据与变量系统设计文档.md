# 数据与变量系统设计文档

## 1. 文档目标

本文档定义测试 Agent 中“数据与变量系统”的职责、边界、核心对象与第一版落地方案。

这个模块的目标不是简单做一个 `${var}` 替换器，而是建立一套能支撑接口自动化测试闭环的数据流转系统，用来解决：

- 环境变量管理
- 鉴权数据注入
- 前置接口结果传递
- 动态测试数据生成
- 执行期变量解析
- 变量作用域隔离

最终让测试 Agent 生成的 case 不只是“能看”，而是真正“能跑”。

---

## 2. 在整体架构中的位置

数据与变量系统位于：

- `测试用例生成模块`
- `执行引擎`
- `编排系统`

之间的中间层位置。

它主要连接以下链路：

```text
TestCase
-> VariableResolver
-> ExecutionContext
-> RequestBuilder
-> HttpExecutor
-> ResponseExtractor
-> ContextStore
-> 下一条 TestCase
```

可以理解为：

`数据与变量系统负责让单条 case 变成可连续执行的测试流程`

---

## 3. 核心设计原则

### 3.1 变量解析必须由本地代码完成

变量替换、响应提取、作用域管理这些能力必须由本地代码稳定实现，而不是依赖 AI 临时推理。

原因很直接：

- 需要稳定
- 需要低延迟
- 需要可追踪
- 需要可重复执行

### 3.2 区分“静态配置”和“执行期数据”

第一版一定要把两类东西分开：

- 静态配置：环境地址、默认 token、固定 Header
- 执行期数据：上一步返回的 userId、orderId、临时 token

否则后面很容易把配置和运行时上下文混在一起。

### 3.3 变量必须有作用域

变量系统不能只是一个全局 map。

至少要支持：

- 环境级作用域
- 任务级作用域
- 测试套件级作用域
- 单次执行级作用域

### 3.4 变量必须可追踪来源

每个变量都最好能回答：

- 这个值从哪来
- 是谁生成的
- 在什么时候写入的
- 当前是否可见

否则调试接口链路时会非常痛苦。

### 3.5 第一版优先解决“接口链路能跑通”

第一版不追求做成通用工作流变量平台，优先把接口自动化测试里的关键数据链路做稳。

---

## 4. 数据与变量系统要解决的问题

这个模块至少要解决以下问题：

1. 如何管理多环境配置
2. 如何在请求中引用变量
3. 如何从响应中提取变量
4. 如何让一个接口的结果传递给后续接口
5. 如何生成动态值，如时间戳、随机数、唯一 ID
6. 如何隔离不同任务/不同测试套件的数据上下文
7. 如何记录变量来源与覆盖关系

---

## 5. 总体设计

建议采用：

`配置层 + 运行时上下文层 + 变量解析层 + 响应提取层`

的四层结构。

整体流程如下：

```text
EnvironmentConfig
+ TaskInputVariables
+ CaseVariables
-> ExecutionContext
-> VariableResolver
-> RequestBuilder
-> HttpExecutor
-> ResponseExtractor
-> VariableWriteBack
-> Updated ExecutionContext
```

四层职责建议：

- 配置层：保存环境变量和固定配置
- 运行时上下文层：保存任务执行中的动态变量
- 变量解析层：负责模板替换和表达式求值
- 响应提取层：负责把接口响应写回上下文

---

## 6. 模块拆分建议

第一版建议拆成以下 8 个组件：

- `EnvironmentConfigManager`
- `ContextStore`
- `ExecutionContextBuilder`
- `VariableResolver`
- `DynamicValueProvider`
- `ResponseExtractor`
- `VariableWriteBackService`
- `ScopeManager`

### 6.1 EnvironmentConfigManager

负责管理环境级配置。

例如：

- baseUrl
- 默认 Header
- 默认 token
- API key
- 数据库连接标识
- 环境名称

### 6.2 ContextStore

负责运行时变量读写。

它应该是执行链路里的核心变量存储。

建议能力：

- 读变量
- 写变量
- 按作用域查询
- 查看变量来源
- 支持变量覆盖

### 6.3 ExecutionContextBuilder

负责把多来源数据合并成一次执行所需的 `ExecutionContext`。

来源包括：

- 环境配置
- 任务输入
- 测试套件共享变量
- case 自带变量
- 上游步骤输出变量

### 6.4 VariableResolver

负责解析请求中的变量表达式。

例如把：

- `${env.baseUrl}`
- `${task.userId}`
- `${step.createOrder.orderId}`
- `${fn.nowMillis()}`

解析成最终值。

### 6.5 DynamicValueProvider

负责生成动态值。

第一版建议支持：

- 当前时间
- 时间戳
- UUID
- 随机字符串
- 随机数字

### 6.6 ResponseExtractor

负责从响应中提取变量。

例如：

- 从 JSON Body 提取 `$.data.id`
- 从 Header 提取 `Set-Cookie`
- 从响应码提取执行状态

### 6.7 VariableWriteBackService

负责把提取出的变量回写到上下文中。

需要支持：

- 写入任务级上下文
- 写入套件级上下文
- 写入当前步骤命名空间

### 6.8 ScopeManager

负责定义和管理变量作用域与可见性。

---

## 7. 变量来源设计

第一版建议支持 6 类变量来源：

- `ENV`
- `TASK`
- `SUITE`
- `CASE`
- `STEP_OUTPUT`
- `DYNAMIC`

### 7.1 ENV

环境级变量。

典型内容：

- `baseUrl`
- `defaultToken`
- `tenantId`
- `appKey`

这类变量通常在任务开始前就已确定。

### 7.2 TASK

任务级变量。

典型内容：

- 用户手工输入的业务参数
- 本次任务指定的账号
- 本次任务目标模块

### 7.3 SUITE

测试套件级共享变量。

适用于一批 case 共享的数据，如：

- 登录态
- 公共用户 ID
- 共享订单号

### 7.4 CASE

单条测试用例自带变量。

例如：

- 当前 case 的特定输入值
- 某条 case 专属断言参数

### 7.5 STEP_OUTPUT

由前置步骤或前一个接口响应提取出的变量。

这是接口自动化链路里最关键的一类变量。

例如：

- `createUser.userId`
- `createOrder.orderId`
- `login.accessToken`

### 7.6 DYNAMIC

运行时动态函数生成的变量。

例如：

- 当前时间
- 随机值
- 唯一请求号

---

## 8. 作用域设计

建议第一版支持 4 层作用域：

- `ENV_SCOPE`
- `TASK_SCOPE`
- `SUITE_SCOPE`
- `STEP_SCOPE`

### 8.1 作用域说明

- `ENV_SCOPE`：整个环境共享，只读为主
- `TASK_SCOPE`：本次任务全局可见
- `SUITE_SCOPE`：当前测试套件内可见
- `STEP_SCOPE`：当前步骤或某个命名步骤输出可见

### 8.2 变量可见性建议

建议采用“由近到远覆盖”的解析顺序：

1. `CASE` / 当前步骤局部变量
2. `STEP_OUTPUT`
3. `SUITE`
4. `TASK`
5. `ENV`

也就是说，离当前执行上下文越近，优先级越高。

### 8.3 命名空间建议

为避免变量名冲突，建议支持命名空间形式：

- `${env.baseUrl}`
- `${task.userId}`
- `${suite.sharedToken}`
- `${step.login.accessToken}`
- `${case.orderNo}`

这样调试和可读性都会更好。

---

## 9. 变量表达式设计

第一版建议先支持简单、可控的模板表达式，不要一上来引入复杂脚本引擎。

建议支持三类表达式：

- 命名空间变量引用
- 动态函数调用
- 简单路径引用

### 9.1 变量引用

格式建议：

```text
${env.baseUrl}
${task.userId}
${suite.token}
${step.createOrder.orderId}
${case.requestId}
```

### 9.2 动态函数

格式建议：

```text
${fn.uuid()}
${fn.nowMillis()}
${fn.randomInt(1000,9999)}
${fn.randomString(8)}
```

### 9.3 响应提取表达式

建议支持：

- JSONPath
- Header Key
- Status Code

例如：

```text
body: $.data.id
header: Set-Cookie
status: code
```

---

## 10. ResponseExtractor 设计

这是接口测试平台非常关键的一层。

它要解决的问题是：

`如何把“执行结果”转成“后续步骤可用数据”`

### 10.1 提取来源

第一版建议支持：

- `BODY_JSON`
- `HEADER`
- `STATUS_CODE`

### 10.2 提取规则对象

建议定义 `ExtractionRule`：

```text
ruleId
sourceType
sourcePath
targetScope
targetKey
required
defaultValue
```

示例：

- 从响应体 `$.data.userId` 提取到 `task.userId`
- 从 Header `Authorization` 提取到 `suite.token`

### 10.3 失败策略

提取失败时建议支持三种策略：

- `FAIL_FAST`
- `WRITE_NULL`
- `WRITE_DEFAULT`

第一版默认推荐：

- 必填提取：`FAIL_FAST`
- 非关键提取：`WRITE_NULL`

---

## 11. ExecutionContext 设计

建议单独定义 `ExecutionContext` 作为执行期统一上下文对象。

建议字段：

```text
taskId
environment
envVariables
taskVariables
suiteVariables
stepVariables
authContext
runtimeFlags
metadata
```

这个对象是：

- `VariableResolver`
- `RequestBuilder`
- `AuthInjector`
- `ResponseExtractor`

之间的桥梁。

---

## 12. 变量覆盖与审计

变量系统后面很容易出问题的一点，就是“谁把谁覆盖了”。

所以建议第一版就保留基础审计能力。

### 12.1 VariableEntry

建议每个变量保存以下元信息：

```text
key
value
sourceType
scope
createdAt
updatedAt
createdByStep
overridden
```

### 12.2 审计价值

这样后面可以回答：

- 当前 token 是哪一步写入的
- 为什么 userId 变了
- 某条 case 用到的是旧值还是新值

这对排查执行异常非常有价值。

---

## 13. 第一版动态函数建议

第一版建议控制范围，只提供高频基础函数：

- `uuid()`
- `nowMillis()`
- `nowDate()`
- `randomInt(min,max)`
- `randomString(length)`

暂时不建议第一版就支持：

- 任意脚本执行
- 自定义 Java 代码片段
- 复杂表达式语言

这样更安全，也更容易稳定。

---

## 14. 与其他模块的关系

### 14.1 与测试用例生成模块

测试用例生成模块可以输出变量占位符，而不是硬编码所有输入值。

例如：

- `${task.userId}`
- `${fn.uuid()}`

### 14.2 与执行引擎

执行引擎依赖数据与变量系统完成：

- 请求前变量替换
- 响应后变量回写
- 多步骤数据传递

### 14.3 与编排系统

编排系统决定：

- 哪一步写变量
- 哪一步读变量
- 哪些变量提升为任务级

### 14.4 与记忆系统

记忆系统不应该直接存大量临时变量。

但可以沉淀：

- 项目常用默认参数
- 典型鉴权方式
- 某类接口的高频提取路径模板

---

## 15. Java 包结构建议

建议包结构如下：

```text
com.xxx.agent.variable
com.xxx.agent.variable.context
com.xxx.agent.variable.resolve
com.xxx.agent.variable.extract
com.xxx.agent.variable.dynamic
com.xxx.agent.variable.model
com.xxx.agent.variable.config
```

---

## 16. 第一版落地建议

建议第一版按以下顺序实现：

1. 定义 `ExecutionContext`、`VariableEntry`、`ExtractionRule`
2. 实现 `EnvironmentConfigManager`
3. 实现 `ContextStore`
4. 实现 `VariableResolver`
5. 实现 `DynamicValueProvider`
6. 实现 `ResponseExtractor`
7. 实现 `VariableWriteBackService`
8. 与执行引擎打通

第一版优先跑通两个关键场景：

- 登录接口提取 token，后续接口自动复用
- 创建接口提取 ID，后续查询/更新接口自动复用

---

## 17. 暂时不建议第一版做的内容

第一版先不要做太重的能力：

- 可视化变量编排平台
- 通用公式引擎
- 脚本型表达式执行器
- 跨任务长期变量共享
- 复杂 secrets 托管系统

这些都适合等主链路跑通后再扩展。

---

## 18. 一句话总结

数据与变量系统的核心价值是：

`把环境配置、动态数据、接口响应和执行上下文串成一条稳定可追踪的数据链，让测试用例真正具备自动连续执行能力`
