# 规则系统设计文档

## 1. 文档目标

本文档定义测试 Agent 中“规则系统”的职责、边界、核心模型与第一版落地方案。

规则系统的目标不是替代 AI，而是为测试用例生成模块提供一套：

- 稳定
- 可解释
- 可扩展
- 可批量执行

的基础测试点生成能力。

它主要负责把 `ApiSpec` 中的结构化约束，转换成可执行的基础 `TestCaseDraft`。

---

## 2. 规则系统在整体架构中的位置

规则系统属于 `StructureCaseGenerator` 的核心子模块。

整体关系如下：

```text
ApiSpec
-> ConstraintExtractor
-> RuleEngine
-> ValueFactory
-> TestCaseDraft List
-> CaseNormalizer
-> TestCase
```

职责边界应明确：

- `规则系统`：负责基础结构型 case 生成
- `AI 业务增强`：负责复杂业务 case 补充
- `记忆系统`：负责历史风险 case 补充
- `执行引擎`：负责执行生成后的 case

也就是说：

`规则系统解决“基础覆盖”，不是解决“全部测试智能化”`

---

## 3. 核心设计原则

### 3.1 规则定义与规则执行分离

需要明确区分两个阶段：

- 规则定义阶段：人工设计，必要时可由 AI 辅助
- 规则执行阶段：由本地代码稳定执行

这样可以避免每次运行时都依赖模型重新推理基础场景。

### 3.2 规则必须可解释

每条生成出来的 case，都应该能回答：

- 来自哪条规则
- 命中了哪个字段
- 为什么生成这条 case

否则后续人工审核、调试和治理都会很困难。

### 3.3 规则输出必须结构化

规则系统输出的不应是自然语言描述，而应是标准化 `TestCaseDraft`，至少包含：

- `caseType`
- `title`
- `description`
- `targetField`
- `inputPatch`
- `expectedStatusCode`
- `ruleCode`

### 3.4 规则优先覆盖高价值通用场景

第一版不要试图把所有业务复杂性都塞进规则系统。

规则系统优先覆盖：

- 正常请求
- 必填缺失
- 类型错误
- 边界值
- 非法枚举
- 鉴权缺失

---

## 4. 规则系统要解决的问题

规则系统至少要解决以下问题：

1. 如何识别接口字段约束
2. 如何把字段约束映射成规则执行条件
3. 如何为规则生成合法值与非法值
4. 如何避免生成大量重复或低价值 case
5. 如何把多个规则输出统一成标准结构
6. 如何支持后续扩展更多规则类型

---

## 5. 总体设计

建议采用：

`约束提取 + 规则匹配 + 值生成 + 草案标准化`

的四段式结构。

主流程如下：

```text
ApiSpec
-> ConstraintExtractor
-> RuleContextBuilder
-> RuleEngine
   -> RuleMatcher
   -> RuleExecutor
   -> ValueFactory
-> DraftDeduplicator
-> TestCaseDraft List
```

建议流程说明：

1. 从 `ApiSpec` 提取字段约束
2. 构造统一 `RuleContext`
3. 根据规则适用条件筛选可执行规则
4. 由规则调用 `ValueFactory` 生成测试值
5. 输出 `TestCaseDraft`
6. 做去重与低价值裁剪

---

## 6. 模块拆分建议

第一版建议拆成以下 7 个组件：

- `ConstraintExtractor`
- `RuleContextBuilder`
- `RuleEngine`
- `RuleMatcher`
- `RuleExecutor`
- `ValueFactory`
- `DraftDeduplicator`

### 6.1 ConstraintExtractor

负责从 `ApiSpec` 中提取统一约束信息。

例如提取：

- 字段名
- 字段路径
- 字段位置
- 字段类型
- 是否必填
- 默认值
- 枚举约束
- 最小值 / 最大值
- 最小长度 / 最大长度
- 格式约束

### 6.2 RuleContextBuilder

负责构造规则执行上下文 `RuleContext`。

上下文建议包含：

- 接口基础信息
- 字段约束列表
- 鉴权要求
- 默认合法请求样本
- 规则执行策略

### 6.3 RuleEngine

规则系统主入口，负责协调整个规则执行流程。

职责：

- 加载内置规则
- 按顺序匹配规则
- 调用规则执行
- 聚合输出结果

### 6.4 RuleMatcher

负责判断某条规则是否适用于当前接口或字段。

例如：

- 只有必填字段才执行“缺失必填”规则
- 只有有枚举约束的字段才执行“非法枚举”规则
- 只有有鉴权要求的接口才执行“鉴权缺失”规则

### 6.5 RuleExecutor

负责执行规则逻辑并产出 `TestCaseDraft`。

这部分逻辑应保持纯本地代码、纯函数化、可测试。

### 6.6 ValueFactory

负责为规则生成：

- 合法值
- 非法值
- 边界值
- 缺失值
- 空值
- 特殊格式值

### 6.7 DraftDeduplicator

负责对生成结果做去重与裁剪。

例如避免：

- 同一字段被多个规则生成高度重复 case
- 同一类边界 case 数量过多
- 标题不同但本质相同的重复 case

---

## 7. 核心对象模型

### 7.1 RuleContext

`RuleContext` 是规则执行的统一输入。

建议字段：

```text
apiId
apiName
httpMethod
path
authRequired
parameterConstraints
bodyConstraints
baseValidRequest
generationConfig
```

### 7.2 FieldConstraint

建议统一抽象字段约束对象：

```text
fieldPath
fieldName
location
dataType
required
nullable
enumValues
minValue
maxValue
minLength
maxLength
pattern
format
exampleValue
defaultValue
```

其中 `location` 可取：

- `PATH`
- `QUERY`
- `HEADER`
- `BODY`

### 7.3 TestCaseDraft

建议规则系统先输出 `TestCaseDraft`，再由上层标准化。

建议字段：

```text
draftId
caseType
title
description
ruleCode
source
targetField
inputPatch
expectedStatusCode
expectedErrorCode
tags
priority
```

其中：

- `source` 固定为 `STRUCTURE`
- `inputPatch` 表示相对“合法默认请求”的改动

### 7.4 RuleDefinition

建议每条规则统一抽象为：

```text
ruleCode
ruleName
supportedCaseType
priority
fieldLevel
matchCondition
execute(context) -> List<TestCaseDraft>
```

---

## 8. 规则分类设计

第一版建议分为两大类：

- 接口级规则
- 字段级规则

### 8.1 接口级规则

作用于整个接口，例如：

- `NORMAL_REQUEST_RULE`
- `AUTH_MISSING_RULE`

### 8.2 字段级规则

作用于某个字段，例如：

- `REQUIRED_MISSING_RULE`
- `INVALID_TYPE_RULE`
- `BOUNDARY_VALUE_RULE`
- `INVALID_ENUM_RULE`

这样拆分后更容易扩展，也更适合后续做规则优先级控制。

---

## 9. 第一版内置规则建议

第一版建议固定内置以下规则：

- `NORMAL_REQUEST_RULE`
- `REQUIRED_MISSING_RULE`
- `INVALID_TYPE_RULE`
- `BOUNDARY_VALUE_RULE`
- `INVALID_ENUM_RULE`
- `AUTH_MISSING_RULE`

### 9.1 NORMAL_REQUEST_RULE

目标：

- 为每个接口生成 1 条基准正常 case

规则说明：

- 基于 `baseValidRequest` 直接生成标准请求
- 用于验证接口最基本的可用性

### 9.2 REQUIRED_MISSING_RULE

目标：

- 针对必填字段生成“缺失字段” case

适用条件：

- 字段 `required = true`

执行方式：

- 从合法请求中移除目标字段

### 9.3 INVALID_TYPE_RULE

目标：

- 针对类型明确的字段生成“类型错误” case

适用条件：

- 字段存在清晰 `dataType`

执行方式：

- string 字段填 number
- number 字段填 string
- boolean 字段填非法文本

### 9.4 BOUNDARY_VALUE_RULE

目标：

- 针对数值和长度约束字段生成边界 case

适用条件：

- 有 `min/max` 或 `minLength/maxLength`

执行方式：

- 生成 `min`
- 生成 `max`
- 生成 `min-1`
- 生成 `max+1`

第一版可做适度裁剪，不必所有字段都生成 4 条。

### 9.5 INVALID_ENUM_RULE

目标：

- 针对枚举字段生成非法枚举值 case

适用条件：

- `enumValues` 非空

执行方式：

- 生成不在枚举集合中的值

### 9.6 AUTH_MISSING_RULE

目标：

- 针对需要鉴权的接口生成“缺少鉴权信息” case

适用条件：

- `authRequired = true`

执行方式：

- 移除 token / API key / auth header

---

## 10. ValueFactory 设计

`ValueFactory` 是规则系统的关键基础设施。

它不负责业务推理，只负责为规则稳定生产测试值。

### 10.1 职责

需要支持：

- `createValidValue`
- `createMissingValueMarker`
- `createInvalidTypeValue`
- `createBoundaryValues`
- `createInvalidEnumValue`
- `createEmptyValue`

### 10.2 值生成原则

值生成需要满足：

- 可复用
- 可解释
- 尽量稳定
- 尽量不引入额外业务噪音

例如：

- 合法字符串：`test_value`
- 合法数字：`100`
- 非法 number：`"abc"`
- 非法 string：`12345`
- 非法枚举：`"INVALID_ENUM_VALUE"`

### 10.3 合法值来源优先级

建议合法值来源按优先级取：

1. `exampleValue`
2. `defaultValue`
3. 知识库推荐默认值
4. 类型默认值模板

这样生成出的正常 case 会更接近真实业务输入。

### 10.4 特殊值策略

第一版建议单独支持以下特殊值：

- `null`
- 空字符串
- 空数组
- 超长字符串
- 非法日期格式
- 特殊字符字符串

这些值后续可以支持更多规则复用。

---

## 11. 规则匹配与执行策略

### 11.1 执行顺序建议

建议按以下顺序执行：

1. `NORMAL_REQUEST_RULE`
2. `AUTH_MISSING_RULE`
3. 字段级规则
4. 去重裁剪

字段级规则内部可按：

1. `REQUIRED_MISSING_RULE`
2. `INVALID_TYPE_RULE`
3. `BOUNDARY_VALUE_RULE`
4. `INVALID_ENUM_RULE`

### 11.2 优先级机制

每条规则建议带 `priority`。

原因是后续可能会出现：

- 同类规则多个版本
- 不同项目启用不同规则策略
- 某些高成本规则需要降级执行

### 11.3 裁剪机制

为避免 case 爆炸，建议加三类裁剪：

- 单接口最大生成数量限制
- 单字段最大边界 case 数限制
- 重复语义 case 合并

例如第一版可设：

- 单接口结构 case 不超过 `20`
- 单字段边界 case 不超过 `2~4`

---

## 12. 规则配置化设计

第一版规则建议“代码内置 + 配置开关”。

不建议一开始就做复杂 DSL。

建议支持以下配置能力：

- 启用哪些规则
- 单接口最大 case 数
- 是否开启激进边界生成
- 是否开启空值测试
- 是否对 GET / POST 使用不同策略

配置示意：

```yaml
caseRule:
  enabledRules:
    - NORMAL_REQUEST_RULE
    - REQUIRED_MISSING_RULE
    - INVALID_TYPE_RULE
    - BOUNDARY_VALUE_RULE
    - INVALID_ENUM_RULE
    - AUTH_MISSING_RULE
  maxCasesPerApi: 20
  maxBoundaryCasesPerField: 4
  enableNullCase: true
  aggressiveBoundaryMode: false
```

---

## 13. 与其他模块的协作关系

### 13.1 与接口自动分析模块

规则系统依赖接口分析模块提供高质量 `ApiSpec`。

如果 `ApiSpec` 约束缺失，规则系统生成能力会明显下降。

### 13.2 与测试用例生成模块

规则系统是 `StructureCaseGenerator` 的底层引擎。

关系可以理解为：

`测试用例生成模块` 决定总体流程  
`规则系统` 负责产出基础结构型 case

### 13.3 与执行引擎

规则系统输出的 `TestCaseDraft / TestCase` 需要被执行引擎直接消费。

因此字段设计必须兼容：

- 请求构建
- 变量解析
- 断言引擎

### 13.4 与记忆系统

规则系统本身不依赖长期记忆做主决策。

但可以使用记忆系统提供的：

- 项目默认合法值偏好
- 某类字段常见风险标签

作为值生成增强信息。

---

## 14. Java 包结构建议

建议包结构如下：

```text
com.xxx.agent.casegen.rule
com.xxx.agent.casegen.rule.engine
com.xxx.agent.casegen.rule.matcher
com.xxx.agent.casegen.rule.executor
com.xxx.agent.casegen.rule.definition
com.xxx.agent.casegen.constraint
com.xxx.agent.casegen.value
com.xxx.agent.casegen.model
```

---

## 15. 第一版落地建议

建议第一版按以下顺序实现：

1. 定义 `FieldConstraint`、`RuleContext`、`TestCaseDraft`
2. 实现 `ConstraintExtractor`
3. 实现 `ValueFactory`
4. 实现 6 条内置规则
5. 实现 `RuleEngine`
6. 实现 `DraftDeduplicator`
7. 接入 `StructureCaseGenerator`

这样可以最快跑通：

`ApiSpec -> 结构型基础 case`

---

## 16. 暂时不建议第一版做的内容

第一版先不要做太重的能力：

- 规则 DSL 平台
- 可视化规则编排界面
- 规则热更新中心
- 复杂表达式语言
- 业务规则全配置化

这些都适合在第一版跑通后再演进。

---

## 17. 一句话总结

规则系统的核心价值不是“智能”，而是：

`把接口约束稳定翻译成基础测试点，用可解释、可执行、可扩展的方式保证接口测试的底层覆盖率`
