# 工具编排模板设计文档

## 1. 文档目标

本文档定义一个适用于测试 Agent 的工具编排模板，用于支撑以下场景：

- 解析需求文档 / OpenAPI
- 生成测试用例
- 执行接口测试
- 分析测试结果
- 根据结果继续补充执行或结束任务

这套模板的目标不是实现一个通用大而全的 DAG 引擎，而是先实现一个可持续迭代的 `Agent Workflow` 主编排骨架。

---

## 2. 设计原则

### 2.1 主编排采用 Agent Loop

主编排采用循环式决策模型，而不是一次性静态编排全流程。

核心闭环如下：

`构建上下文 -> 决策下一步 -> 路由工具 -> 执行工具 -> 收集观察结果 -> 更新状态 -> 判断是否继续`

### 2.2 子流程可逐步 DAG 化

对于稳定、可批量、可并行的执行链路，可以在后续下沉为 DAG / Job Flow，例如：

- 批量执行接口测试
- 多环境回归
- 数据准备 -> 执行 -> 报告汇总

### 2.3 编排与工具解耦

编排层不直接依赖某个具体工具实现，而是通过统一接口调用工具。

这样后续替换执行器、增加新工具、接入新模型时，主编排层不需要大改。

### 2.4 编排与记忆分层

编排系统负责“下一步做什么”，记忆系统负责“拿什么上下文来做”。

两者协作，但职责分离。

---

## 3. 总体架构

### 3.1 模块组成

工具编排系统建议拆成以下模块：

- `OrchestrationEngine`
- `TaskStateStore`
- `ContextBuilder`
- `Planner`
- `ToolRouter`
- `ExecutionController`
- `ObservationCollector`
- `LoopDecider`

### 3.2 模块关系

```text
Task Input
-> OrchestrationEngine
-> ContextBuilder
-> Planner
-> ToolRouter
-> ExecutionController
-> ObservationCollector
-> TaskStateStore / Memory
-> LoopDecider
-> Continue or Finish
```

---

## 4. 模块职责设计

### 4.1 OrchestrationEngine

这是整个编排系统的入口与主循环控制器。

职责：

- 接收任务请求
- 初始化任务状态
- 驱动主循环执行
- 协调各模块调用顺序
- 统一控制任务结束、失败、中断

它不负责具体业务决策，而是负责把整个流程跑起来。

### 4.2 TaskStateStore

这是编排系统的状态底座。

职责：

- 保存任务全局状态
- 保存当前阶段与已完成步骤
- 保存最近一次工具执行记录
- 保存重试状态、失败状态、终止原因
- 支持恢复任务与断点续跑

建议至少记录以下信息：

- `taskId`
- `status`
- `currentStage`
- `currentStep`
- `retryCount`
- `lastObservation`
- `errorCode`
- `errorMessage`
- `nextActionHint`

### 4.3 ContextBuilder

负责为当前轮次构建可执行上下文。

上下文来源可以包括：

- 用户输入
- 当前 TaskState
- Session Memory
- Task Memory
- Long-term Memory
- 外部文档，如需求、OpenAPI、历史报告

输出结果应统一封装为 `ContextBundle`，供 Planner 和 ToolRouter 使用。

### 4.4 Planner

负责决定当前轮次的“下一步动作”。

Planner 不要求一次性产出全量计划，而是每轮产出一条或几条可执行步骤。

典型输出：

- 解析接口文档
- 生成基础接口测试用例
- 执行某组测试用例
- 对失败结果补充断言分析
- 生成最终测试报告

输出对象建议为 `PlanStep`。

### 4.5 ToolRouter

负责根据 `PlanStep` 选择合适工具。

例如：

- `ANALYZE_OPENAPI` -> `OpenApiParserTool`
- `GENERATE_API_CASES` -> `CaseGeneratorTool`
- `EXECUTE_API_TEST` -> `HttpExecutorTool`
- `ANALYZE_FAILURE` -> `FailureAnalyzerTool`
- `GENERATE_REPORT` -> `ReportGeneratorTool`

ToolRouter 的意义在于让 Planner 只关注“做什么”，不直接关心“具体谁来做”。

### 4.6 ExecutionController

负责真正发起工具执行。

职责：

- 工具入参准备
- 单工具执行
- 多工具并行执行
- 超时控制
- 重试控制
- 失败中断
- 执行日志采集

第一版建议支持两种执行模式：

- `SERIAL`
- `PARALLEL`

后续可增加：

- `BATCH`
- `BACKGROUND`
- `DAG_SUBFLOW`

### 4.7 ObservationCollector

负责把工具原始输出转成标准化观察结果。

例如接口测试执行后，不直接把原始 response 交给主循环，而是整理成统一对象：

- 请求参数摘要
- 响应状态码
- 响应体摘要
- 断言结果
- 错误信息
- 可沉淀经验

输出对象建议为 `Observation`。

### 4.8 LoopDecider

负责判断当前任务是否继续。

判断维度包括：

- 当前目标是否完成
- 是否还有待执行步骤
- 是否需要基于结果追加一步分析
- 是否达到最大轮次
- 是否达到重试上限
- 是否出现不可恢复错误

输出建议为：

- `CONTINUE`
- `FINISH_SUCCESS`
- `FINISH_FAIL`
- `WAITING_EXTERNAL_INPUT`

---

## 5. 主循环模板

建议主循环模板如下：

```text
1. 接收任务输入
2. 初始化 TaskState
3. 构建当前 ContextBundle
4. Planner 生成当前 PlanStep
5. ToolRouter 为 PlanStep 匹配 ToolDescriptor
6. ExecutionController 执行工具
7. ObservationCollector 生成 Observation
8. TaskStateStore 更新任务状态
9. 写回 Memory
10. LoopDecider 判断是否继续
11. 若继续，则进入下一轮
12. 若结束，则输出最终结果
```

可以进一步抽象成：

```text
while (task not finished) {
  context = buildContext(taskState, memory, materials)
  step = planner.planNext(context)
  tool = toolRouter.route(step)
  result = executionController.execute(tool, step, context)
  observation = observationCollector.collect(result)
  taskState = taskStateStore.update(taskState, step, observation)
  memory.write(taskState, observation)
  decision = loopDecider.decide(taskState, observation)
}
```

---

## 6. 编排核心对象模板

### 6.1 TaskState

建议字段：

- `taskId`
- `taskType`
- `status`
- `currentStage`
- `currentStepId`
- `stepHistory`
- `retryCount`
- `maxRetry`
- `latestObservation`
- `createdAt`
- `updatedAt`

### 6.2 ContextBundle

建议字段：

- `taskInput`
- `taskStateSnapshot`
- `sessionMemory`
- `taskMemory`
- `longTermMemory`
- `externalMaterials`
- `runtimeOptions`

### 6.3 PlanStep

建议字段：

- `stepId`
- `stepType`
- `goal`
- `inputRef`
- `executionMode`
- `expectedOutput`
- `priority`

### 6.4 ToolDescriptor

建议字段：

- `toolName`
- `toolType`
- `inputSchema`
- `executionMode`
- `timeoutMs`
- `retryPolicy`
- `concurrencySafe`

### 6.5 ExecutionResult

建议字段：

- `success`
- `toolName`
- `rawOutput`
- `errorCode`
- `errorMessage`
- `durationMs`
- `metadata`

### 6.6 Observation

建议字段：

- `observationType`
- `summary`
- `assertionResult`
- `riskLevel`
- `nextSuggestion`
- `memoryCandidates`

---

## 7. 推荐的 Step 类型模板

第一版不要把 Step 类型设计得太泛，建议先固定几类核心步骤：

- `ANALYZE_INPUT`
- `ANALYZE_OPENAPI`
- `GENERATE_FUNCTION_CASES`
- `GENERATE_API_CASES`
- `EXECUTE_API_TEST`
- `ANALYZE_TEST_RESULT`
- `GENERATE_REPORT`

后续再扩展：

- `GENERATE_TEST_DATA`
- `EXECUTE_UI_TEST`
- `CODE_CONTEXT_ANALYSIS`
- `BUG_ROOT_CAUSE_ANALYSIS`

---

## 8. 推荐的 Tool 分类模板

工具建议分层管理：

### 8.1 Analysis Tools

- 文档解析工具
- OpenAPI 解析工具
- 代码上下文分析工具

### 8.2 Generation Tools

- 测试用例生成工具
- 断言生成工具
- 报告生成工具

### 8.3 Execution Tools

- HTTP 执行工具
- 测试数据准备工具
- 浏览器执行工具

### 8.4 Diagnose Tools

- 失败分析工具
- 日志检索工具
- 结果归因工具

---

## 9. 异常与恢复机制模板

编排系统第一版就应该保留恢复位点，而不是只考虑成功路径。

### 9.1 可恢复异常

例如：

- 工具超时
- 外部接口临时失败
- 测试环境偶发异常
- 输出不满足格式要求

处理方式：

- 重试
- 降级工具
- 重新规划当前步骤

### 9.2 不可恢复异常

例如：

- 输入材料严重缺失
- 核心配置错误
- 多次重试仍失败

处理方式：

- 标记任务失败
- 输出失败原因
- 终止主循环

### 9.3 恢复策略建议

建议抽象成 `RetryPolicy`：

- `NONE`
- `SIMPLE_RETRY`
- `EXPONENTIAL_BACKOFF`
- `REPLAN_AFTER_FAIL`

---

## 10. 与 ClaudeCode 编排思想的关系

该模板借鉴了 ClaudeCode 的几个核心思想：

- 使用 `agent loop` 作为主编排结构
- 工具执行结果回灌主循环
- 区分串行工具与可并发工具
- 把高阶能力也当成工具来编排

但不直接照搬 ClaudeCode 的实现，原因是：

- ClaudeCode 偏通用 coding agent
- 当前系统偏测试平台任务执行
- 当前方案更强调任务状态、测试执行记录、结果分析和恢复机制

因此这里采用的是：

`ClaudeCode 编排思想 + 测试平台任务状态机`

---

## 11. Java 第一版落地建议

建议包结构如下：

```text
com.xxx.agent.orchestration.engine
com.xxx.agent.orchestration.state
com.xxx.agent.orchestration.context
com.xxx.agent.orchestration.plan
com.xxx.agent.orchestration.route
com.xxx.agent.orchestration.execute
com.xxx.agent.orchestration.observe
com.xxx.agent.orchestration.loop
com.xxx.agent.orchestration.model
```

建议第一版优先实现的接口：

- `OrchestrationEngine`
- `Planner`
- `ToolRouter`
- `ExecutionController`
- `ObservationCollector`
- `LoopDecider`
- `TaskStateRepository`

建议第一版优先跑通的主流程：

`OpenAPI 输入 -> 生成接口测试用例 -> 执行接口测试 -> 结果分析 -> 输出报告`

---

## 12. 一句话总结

工具编排模板的核心，不是把所有步骤预先画成静态 DAG，而是先搭一个可循环决策、可调用工具、可回收观察结果、可持续迭代的主编排骨架。

最终模板可以概括为：

`Agent Workflow 主循环 + 标准化工具路由 + 标准化观察结果 + 可扩展的 DAG 子流程`
