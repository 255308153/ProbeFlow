# 知识库系统设计文档

## 1. 文档目标

本文档定义测试 Agent 中“知识库系统”的职责、边界、核心模块与第一版落地方案。

知识库系统的目标不是做一个泛化文档中心，而是为测试 Agent 提供稳定、可检索、可治理的项目级与团队级知识来源，用来支撑：

- 接口自动分析
- 测试用例生成
- 执行前上下文补充
- 失败分析
- 测试策略复用

它要解决的问题不是“把文档存下来”，而是：

`把项目中原本就存在的业务知识、测试规范和接口补充说明，转成 Agent 可以稳定消费的上下文资产`

---

## 2. 在整体架构中的位置

知识库系统和记忆系统应当是平级模块，而不是互相替代关系。

整体关系如下：

```text
业务文档 / 测试规范 / FAQ / 事故复盘 / 环境说明
-> Knowledge Base System
-> Knowledge Retriever
-> Unified Context Builder
-> Agent Current Context
```

职责边界建议明确：

- `知识库系统`：保存项目原生知识和团队共享知识
- `记忆系统`：保存 Agent 在运行过程中沉淀出的经验
- `代码分析系统`：提供源码结构和接口实现信息
- `统一上下文构建器`：把知识库、记忆、代码上下文按需组装

也就是说：

`知识库回答“项目本来知道什么”，记忆系统回答“Agent 后来学到了什么”`

---

## 3. 核心设计原则

### 3.1 知识库优先承载“稳定知识”

知识库更适合保存：

- 业务流程
- 测试规范
- 接口补充说明
- 环境说明
- 事故复盘
- 错误码说明

不适合承载：

- 当前任务临时状态
- 短期执行中间结果
- 高频变化的调试痕迹

### 3.2 文档内容必须结构化增强

第一版不建议把知识库当作纯文本堆积。

应在文档原文基础上补充结构化元数据，用于：

- 过滤
- 召回
- 重排
- 生命周期治理

### 3.3 检索不应只靠向量

测试 Agent 的知识库往往带有强结构信息，比如：

- system
- module
- api
- businessEntity
- docType

因此第一版应采用混合检索，而不是纯 embedding 相似度。

### 3.4 知识库需要可治理

知识库不是一旦导入就结束。

系统必须考虑：

- 文档版本
- 过期治理
- 来源可信度
- 适用范围

### 3.5 第一版优先服务接口测试主链路

知识库系统第一版不追求覆盖所有企业知识形态，优先服务：

- 接口分析
- 用例生成
- 失败分析

---

## 4. 知识库系统要解决的问题

知识库系统至少要解决以下问题：

1. 如何导入团队已有文档
2. 如何把文档切块并建立结构化索引
3. 如何让不同阶段检索到不同类型知识
4. 如何让知识结果和代码上下文、记忆上下文配合
5. 如何管理文档版本、失效和权威性

---

## 5. 总体设计

建议采用：

`文档导入 + 元数据增强 + 分块索引 + 混合检索 + 上下文组装`

的五段式结构。

整体流程如下：

```text
Raw Documents
-> KnowledgeIngestService
-> Chunker + MetadataExtractor
-> KnowledgeIndex
-> KnowledgeRetriever
-> KnowledgeContextBuilder
-> ContextBundle
```

流程说明：

1. 导入文档
2. 提取元数据
3. 做分块与索引
4. 按阶段检索候选知识
5. 重排并组装成当前任务上下文

---

## 6. 模块拆分建议

第一版建议拆成以下 7 个组件：

- `KnowledgeIngestService`
- `KnowledgeChunker`
- `KnowledgeMetadataExtractor`
- `KnowledgeDocumentRepository`
- `KnowledgeRetriever`
- `KnowledgeReranker`
- `KnowledgeContextBuilder`

### 6.1 KnowledgeIngestService

负责文档导入与标准化。

支持来源建议包括：

- Markdown
- PRD
- Wiki 导出文档
- 测试规范文档
- FAQ
- 事故复盘

### 6.2 KnowledgeChunker

负责文档切块。

切块目标不是越碎越好，而是保留业务语义完整性。

建议优先按：

- 一级/二级标题
- 段落块
- 列表块
- 表格块

进行语义分块。

### 6.3 KnowledgeMetadataExtractor

负责提取或补充文档元数据。

建议提取内容包括：

- 系统名
- 模块名
- 文档类型
- 业务实体
- 标签
- 适用阶段
- 来源类型
- 权威等级
- 更新时间

### 6.4 KnowledgeDocumentRepository

负责保存：

- 原始文档
- chunk 数据
- 元数据
- embedding
- 版本信息

### 6.5 KnowledgeRetriever

负责对知识库进行多路召回。

召回方式应包括：

- 结构过滤
- 标签过滤
- 语义召回
- 文档类型过滤

### 6.6 KnowledgeReranker

负责对召回结果进行重排。

重排维度建议包括：

- 相关性
- 权威性
- 新鲜度
- 适用阶段

### 6.7 KnowledgeContextBuilder

负责把知识检索结果组装成当前 Agent 可消费的结构化上下文。

它不是简单返回片段列表，而是返回：

- 业务流程摘要
- 规则摘要
- 风险摘要
- 推荐测试关注点

---

## 7. 知识内容分类建议

第一版建议先支持以下 7 类知识文档：

- `business_flow`
- `api_note`
- `test_spec`
- `env_guide`
- `error_code_guide`
- `incident_postmortem`
- `domain_rule`

### 7.1 business_flow

用于描述业务流程、上下游关系、状态流转。

### 7.2 api_note

用于补充 OpenAPI/代码里没有写清楚的接口业务语义。

### 7.3 test_spec

用于描述测试规范、断言规则、测试策略。

### 7.4 env_guide

用于描述环境说明、鉴权方式、公共前置条件。

### 7.5 error_code_guide

用于描述错误码、业务失败语义和典型处理方式。

### 7.6 incident_postmortem

用于描述历史事故、风险模式、已知坑点。

### 7.7 domain_rule

用于描述行业规则、业务边界、合规限制。

---

## 8. 文档写法建议

第一版建议采用：

`Markdown 正文 + frontmatter 元数据`

的形式。

示例：

```md
---
docId: kb-order-create
title: 订单创建接口业务规则
system: test-agent
module: order
docType: api_note
bizEntity: order
tags: [api, order, auth, validation]
authority: high
sourceType: wiki
applicableStages: [api_analysis, case_generation]
updatedAt: 2026-07-02
---

# 订单创建接口业务规则

...
```

这样做的价值在于：

- 元数据先过滤
- 正文再分块检索
- 检索后更容易重排

---

## 9. 核心对象模型

### 9.1 KnowledgeDocument

建议字段：

```text
docId
title
system
module
docType
bizEntity
sourceType
authority
version
status
updatedAt
metadata
rawContent
```

### 9.2 KnowledgeChunk

建议字段：

```text
chunkId
docId
chunkTitle
chunkContent
chunkOrder
tags
embedding
tokenCount
applicableStages
```

### 9.3 KnowledgeQuery

建议字段：

```text
queryText
system
module
apiPath
httpMethod
bizEntity
stage
tags
limit
```

### 9.4 KnowledgeHit

建议字段：

```text
chunkId
docId
title
docType
score
authority
updatedAt
reason
snippet
```

---

## 10. 存储设计建议

第一版建议采用：

- `PostgreSQL`：保存文档、chunk、元数据、版本
- `pgvector`：保存 chunk embedding

第一版不强依赖 ES / Milvus。

原因是：

- 量级通常还不大
- 先用 PG + pgvector 足够
- 系统部署更简单

后续若知识量变大，再扩展：

- Elasticsearch
- 外部向量库

---

## 11. 与其他模块的关系

### 11.1 与接口自动分析模块

知识库系统为接口分析阶段补充：

- 业务流程
- 鉴权规则
- 前后置依赖

### 11.2 与测试用例生成模块

知识库系统为 case 生成阶段补充：

- 风险点
- 边界规则
- 测试规范
- 历史事故经验文档

### 11.3 与断言系统

知识库系统可提供：

- 成功码规范
- 错误码说明
- 断言模板参考

### 11.4 与记忆系统

知识库和记忆系统是平级关系。

二者差异：

- 知识库：静态或半静态项目知识
- 记忆系统：运行中提炼出的经验知识

统一上下文阶段再合并消费。

---

## 12. 第一版落地建议

建议第一版按以下顺序实现：

1. 定义 `KnowledgeDocument`、`KnowledgeChunk`、`KnowledgeQuery`
2. 支持 Markdown + frontmatter 导入
3. 实现按标题和段落切块
4. 实现 PostgreSQL + pgvector 存储
5. 实现结构过滤 + 标签过滤 + 向量召回
6. 实现 `KnowledgeContextBuilder`
7. 接入接口分析和 case 生成阶段

第一版优先支持的场景：

- 某接口分析时自动补业务流程说明
- 某接口生成 case 时自动补测试规范和风险点
- 某失败结果分析时自动补错误码说明与事故复盘

---

## 13. 暂时不建议第一版做的内容

第一版先不要做太重的能力：

- 可视化知识编辑器
- 多知识源实时同步平台
- 企业级权限细粒度控制
- 复杂图谱自动构建
- 大规模全文搜索集群

这些都适合等主链路跑通后再扩展。

---

## 14. 一句话总结

知识库系统的核心价值是：

`把团队和项目已有的业务知识、测试规范与接口补充说明，转成可检索、可治理、可按阶段注入的 Agent 外部上下文资产`
