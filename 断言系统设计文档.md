# 断言系统设计文档

## 1. 文档目标

本文档定义测试 Agent 中“断言系统”的职责、边界、核心模型与第一版落地方案。

断言系统的目标不是简单判断“接口有没有返回 200”，而是建立一套：

- 可执行
- 可解释
- 可扩展
- 可复用

的结果判定能力。

它负责把：

- `TestCase` 中的断言要求
- 系统自动补充的基础断言
- 响应结果与执行上下文

转换为：

- 结构化 `AssertionResult`
- 整体执行结论
- 可追踪失败原因

---

## 2. 在整体架构中的位置

断言系统属于执行引擎中的核心子模块，对应执行引擎里的 `AssertionEngine`。

整体关系如下：

```text
TestCase
+ AssertionPlan
+ HttpResponse
+ ExecutionContext
-> AssertionEngine
-> AssertionResult List
-> ExecutionResult
```

职责边界应明确：

- `测试用例生成模块`：决定应该断言什么
- `断言系统`：负责真正执行断言
- `AI 分析层`：负责解释复杂失败与补充建议
- `执行引擎`：负责调用断言系统并汇总结果

也就是说：

`断言系统负责“判定事实”，AI 负责“解释事实”`

---

## 3. 核心设计原则

### 3.1 断言执行必须由本地代码完成

断言是执行事实判定，不适合依赖模型临时理解。

原因包括：

- 需要稳定
- 需要低成本
- 需要高并发
- 需要结果一致
- 需要方便回放和调试

### 3.2 断言对象必须结构化

第一版不要把断言写成自由文本。

断言系统应消费结构化断言定义，例如：

- 状态码等于 200
- JSONPath `$.code` 等于 `0`
- Header 中存在 `traceId`
- 响应时间小于 `1000ms`

### 3.3 断言失败必须可解释

断言结果不能只返回 `true / false`。

至少要能说明：

- 哪条断言失败了
- 期望值是什么
- 实际值是什么
- 失败发生在什么位置

### 3.4 基础断言与业务断言分层

建议把断言分成两层：

- 基础断言：状态码、Header、时间等稳定规则
- 业务断言：业务码、关键字段、业务状态

这样更容易治理，也更方便后续 AI 增强。

### 3.5 第一版优先支持高频断言

第一版不需要一开始就做复杂逻辑编排。

优先支持最常见、最有价值的断言类型：

- 状态码断言
- JSONPath 断言
- Header 断言
- 文本包含断言
- 响应时间断言

---

## 4. 断言系统要解决的问题

断言系统至少要解决以下问题：

1. 如何定义统一断言模型
2. 如何执行不同类型的断言
3. 如何输出统一断言结果
4. 如何支持系统自动补基础断言
5. 如何让断言与变量系统协同
6. 如何把多条断言聚合成整体通过/失败结论

---

## 5. 总体设计

建议采用：

`断言计划构建 + 断言执行链 + 结果聚合`

的三段式结构。

主流程如下：

```text
TestCase
+ HttpResponse
+ ExecutionContext
-> AssertionPlanBuilder
-> AssertionEngine
   -> AssertionDispatcher
   -> AssertionExecutor
-> AssertionResultAggregator
-> AssertionSummary
```

流程说明：

1. 从 `TestCase` 中读取断言定义
2. 系统补充基础断言
3. 构建统一 `AssertionPlan`
4. 根据断言类型分发到不同执行器
5. 输出多条 `AssertionResult`
6. 聚合为整体断言结论

---

## 6. 模块拆分建议

第一版建议拆成以下 7 个组件：

- `AssertionPlanBuilder`
- `AssertionEngine`
- `AssertionDispatcher`
- `AssertionExecutor`
- `AssertionResultAggregator`
- `AssertionValueResolver`
- `AssertionFailureFormatter`

### 6.1 AssertionPlanBuilder

负责构建本次执行的断言计划。

职责：

- 读取 case 自带断言
- 自动补充基础断言
- 统一输出 `AssertionPlan`

### 6.2 AssertionEngine

主入口，负责协调整个断言执行流程。

职责：

- 接收断言计划
- 调度断言执行器
- 收集结果
- 返回断言汇总

### 6.3 AssertionDispatcher

负责根据断言类型路由到对应执行器。

例如：

- `STATUS_CODE` -> `StatusCodeAssertionExecutor`
- `JSON_PATH` -> `JsonPathAssertionExecutor`
- `HEADER` -> `HeaderAssertionExecutor`

### 6.4 AssertionExecutor

表示通用断言执行器接口。

每类断言都实现自己的执行器。

### 6.5 AssertionResultAggregator

负责把多条断言结果聚合成整体结论。

例如：

- 是否全部通过
- 失败数
- 警告数
- 关键断言是否失败

### 6.6 AssertionValueResolver

负责解析断言中的变量表达式。

例如：

- `${task.userId}`
- `${step.createOrder.orderId}`
- `${fn.nowDate()}`

### 6.7 AssertionFailureFormatter

负责格式化失败信息，让结果更适合日志、报告和 AI 分析。

---

## 7. 核心对象模型

### 7.1 AssertionDefinition

建议统一定义断言对象：

```text
assertionId
assertionType
operator
target
expectedValue
required
severity
message
source
```

字段说明：

- `assertionType`：断言类型
- `operator`：比较操作符
- `target`：断言目标，如状态码、JSONPath、Header 名称
- `expectedValue`：期望值
- `required`：是否关键断言
- `severity`：失败严重级别
- `source`：断言来源

### 7.2 AssertionPlan

表示某次接口执行的完整断言计划。

建议字段：

```text
executionId
caseId
assertions
generatedAt
metadata
```

### 7.3 AssertionResult

表示单条断言执行结果。

建议字段：

```text
assertionId
assertionType
passed
severity
target
operator
expectedValue
actualValue
failureReason
message
source
durationMs
```

### 7.4 AssertionSummary

表示整个断言阶段的聚合结果。

建议字段：

```text
totalCount
passedCount
failedCount
warningCount
criticalFailed
overallPassed
results
```

---

## 8. 第一版断言类型设计

第一版建议先支持 5 类断言：

- `STATUS_CODE`
- `JSON_PATH`
- `HEADER`
- `BODY_TEXT`
- `RESPONSE_TIME`

### 8.1 STATUS_CODE

目标：

- 校验 HTTP 状态码是否符合预期

示例：

- 状态码等于 `200`
- 状态码属于 `[200, 201]`

第一版建议至少支持：

- `EQ`
- `IN`

### 8.2 JSON_PATH

目标：

- 校验 JSON 响应体中的字段值

示例：

- `$.code == 0`
- `$.data.id exists`
- `$.data.status in ["SUCCESS","DONE"]`

第一版建议支持：

- `EQ`
- `NE`
- `EXISTS`
- `NOT_EXISTS`
- `IN`
- `CONTAINS`

### 8.3 HEADER

目标：

- 校验响应 Header

示例：

- `traceId exists`
- `Content-Type contains application/json`

第一版建议支持：

- `EXISTS`
- `EQ`
- `CONTAINS`

### 8.4 BODY_TEXT

目标：

- 对非标准 JSON 或文本响应做快速断言

示例：

- 响应文本包含 `success`
- 响应文本不包含 `error`

第一版建议支持：

- `CONTAINS`
- `NOT_CONTAINS`

### 8.5 RESPONSE_TIME

目标：

- 校验接口响应时间

示例：

- 响应时间小于 `1000ms`

第一版建议支持：

- `LT`
- `LTE`

---

## 9. 操作符设计

建议第一版统一操作符集合：

- `EQ`
- `NE`
- `IN`
- `EXISTS`
- `NOT_EXISTS`
- `CONTAINS`
- `NOT_CONTAINS`
- `LT`
- `LTE`
- `GT`
- `GTE`

这样不同断言执行器可以共享一套基础比较语义。

---

## 10. 基础断言自动补充设计

这部分非常重要。

很多 case 即使没有手工写断言，也应该由系统自动补最基础的一层。

### 10.1 自动补充原则

建议第一版默认补以下基础断言：

- 正常类 case 自动补状态码断言
- 鉴权失败类 case 自动补 `401/403` 状态码断言
- 基础 JSON 接口自动补 `Content-Type` 包含 `application/json`

### 10.2 与 caseType 的关系

可以按 `caseType` 补断言：

- `NORMAL` -> 状态码 `200`
- `AUTH_FAILURE` -> 状态码 `401/403`
- `REQUIRED_FIELD_MISSING` -> 状态码 `400`
- `INVALID_TYPE` -> 状态码 `400`

这不是绝对规则，但第一版足够实用。

### 10.3 断言来源标记

自动补充的断言建议 `source = SYSTEM_GENERATED`。

手工或 AI 生成的断言建议可标为：

- `MANUAL`
- `AI_GENERATED`
- `RULE_GENERATED`

这样后续便于分析断言效果。

---

## 11. 断言执行流程设计

建议流程如下：

```text
读取 AssertionPlan
-> 解析 expectedValue 中的变量
-> 按 assertionType 路由执行器
-> 执行比较
-> 生成 AssertionResult
-> 汇总 AssertionSummary
```

更具体的执行步骤：

1. 读取 `AssertionPlan`
2. 解析期望值中的变量和动态函数
3. 获取响应目标值
4. 根据操作符执行比较
5. 生成单条断言结果
6. 聚合出整体断言结论

---

## 12. 与变量系统的协同

断言系统必须和变量系统打通。

因为很多断言的期望值并不是写死的，而是来自前置步骤。

例如：

- `$.data.userId == ${task.userId}`
- `$.data.orderId == ${step.createOrder.orderId}`

因此建议：

- 断言期望值通过 `AssertionValueResolver` 解析
- 断言目标值从响应中获取
- 两边都统一转成可比较类型

---

## 13. 失败结果设计

断言失败的可读性很关键。

建议每条失败结果至少包含：

- 失败断言 ID
- 断言类型
- 目标位置
- 期望值
- 实际值
- 操作符
- 失败原因

示例失败信息：

```text
JSON_PATH assertion failed:
target=$.code
expected=0
actual=1001
operator=EQ
reason=business code mismatch
```

这样的结果更容易：

- 进入报告系统
- 供 AI 做失败分析
- 供记忆系统沉淀高频失败模式

---

## 14. 聚合策略设计

并不是所有断言失败都应该等价看待。

第一版建议支持两层聚合：

- 单条断言结果
- 整体断言结果

### 14.1 严重级别

建议断言带 `severity`：

- `CRITICAL`
- `WARNING`
- `INFO`

### 14.2 整体通过逻辑

建议第一版规则：

- 有任意 `CRITICAL` 失败 -> 整体失败
- 只有 `WARNING` 失败 -> 整体可标记为通过但带警告，或者配置化控制

为了简单起见，第一版也可以先统一：

- 只要有失败就整体失败

后续再细化。

---

## 15. Java 包结构建议

建议包结构如下：

```text
com.xxx.agent.assertion
com.xxx.agent.assertion.engine
com.xxx.agent.assertion.executor
com.xxx.agent.assertion.dispatch
com.xxx.agent.assertion.model
com.xxx.agent.assertion.resolve
com.xxx.agent.assertion.aggregate
```

---

## 16. 第一版落地建议

建议第一版按以下顺序实现：

1. 定义 `AssertionDefinition`、`AssertionResult`、`AssertionSummary`
2. 实现 `AssertionPlanBuilder`
3. 实现 `StatusCodeAssertionExecutor`
4. 实现 `JsonPathAssertionExecutor`
5. 实现 `HeaderAssertionExecutor`
6. 实现 `BodyTextAssertionExecutor`
7. 实现 `ResponseTimeAssertionExecutor`
8. 实现 `AssertionResultAggregator`
9. 与执行引擎打通

第一版优先跑通两个典型场景：

- 正常接口：状态码 + 业务码 + 关键字段断言
- 失败接口：状态码 + 错误码 + 错误信息断言

---

## 17. 暂时不建议第一版做的内容

第一版先不要做太重的能力：

- 复杂逻辑组合断言
- 自定义脚本断言
- SQL 结果断言
- 外部系统回调断言
- 可视化断言编辑器

这些都适合第一版跑通后再扩展。

---

## 18. 一句话总结

断言系统的核心价值是：

`把接口执行结果转成稳定、可解释、可结构化的判定结论，为执行引擎、报告系统和 AI 分析层提供可信的结果基础`
